# Scrapes the Pulse monitor listener's /metrics. See docs/Operations.md.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: pulse
    metrics_path: /metrics
    static_configs:
      - targets: ["pulse:9091"]
